version: "v1.0"
name: Using switch
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: ls
    task:
      jobs:
      - name: List contents
        commands:
          - ls -al
          - echo $SEMAPHORE_PIPELINE_ID

switch:
  - name: Switch Job 1 
    pipeline_path: switch.yml
  - name: Switch Job 2
    pipeline_path: secondSwitch.yml
